classDiagram
    class Task {
        +str taskName
        +int priority
        +float duration
        +str description
        +str frequency
        +bool completed
        +mark_complete()
    }
    class Pet {
        +str petName
        +list taskList
        +add_task(task)
        +remove_task(task)
    }
    class Owner {
        +str ownerName
        +float timeConstraint
        +list petList
        +add_pet(pet)
        +remove_pet(pet)
    }
    class Scheduler {
        +get_all_tasks(owner)
        +schedule_for_owner(owner)
    }
    Owner "1" o-- "*" Pet : owns
    Pet "1" o-- "*" Task : has